# Defence-in-depth — root .gitignore already covers these,
# but we keep them here too in case the server folder is ever
# moved or used as its own git root.

node_modules/
.env
.env.*
!.env.example
data/
*.log
